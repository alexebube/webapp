
language: node_js

node_js:
 -"0.10"

branches:
 only:
  -master

before_install:
 -npm install -g hexo

install:
 -npm install

before_script:
 -git config --global user.name 'Alexander Obi'
 -git config --global user.email 'alexebube@gmail.com'

script:
 -hexo generate

deploy:
 provider: s3
 access_key_id: "AKIAJI2E4OJNJ5V5DGJQ"
 secret_access_key: "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"
 bucket: "S3 Bucket"
 skip_cleanup: true
 local-dir: public
